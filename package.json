{
  "name": "family-budget-prototype",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "lint": "biome lint biome.json package.json vite.config.js src/main.jsx src/lib",
    "format": "biome check --formatter-enabled=true --linter-enabled=false --assist-enabled=false biome.json package.json vite.config.js src/main.jsx src/lib",
    "format:write": "biome format --write biome.json package.json vite.config.js src/main.jsx src/lib",
    "verify": "npm run test && npm run lint && npm run format && npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.104.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@vitejs/plugin-react": "^5.0.0",
    "supabase": "^2.95.3",
    "vite": "^7.0.0"
  }
}
